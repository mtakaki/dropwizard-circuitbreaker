circuitBreaker:
  threshold: 0.5
  rateType: ONE_MINUTE
  customThresholds:
    customName: 0.2

logging:
  level: ERROR

server:
  applicationConnectors:
    - type: http
      port: 0
  adminConnectors:
    - type: http
      port: 0
